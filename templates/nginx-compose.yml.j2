version: "3.3"

services:
  nginx:
    container_name: nginx
    image: nginx:1.22.0
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/var/www:/var/www:ro"
      - "./nginx.conf:/etc/nginx/nginx.conf:ro"
      - "./conf.d:/etc/nginx/conf.d:ro"